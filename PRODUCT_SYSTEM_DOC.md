# 📗 Product System Technical Documentation (v2)

เอกสารนี้สรุปโครงสร้างทั้งหมดของระบบ Product เพื่อใช้ในการเชื่อมต่อกับ Frontend (React/Angular)

---

## 🏗️ 1. Mongoose Model (Data Structure)
**File:** `src/models/product.model.js`

| Field | Type | Description |
| :--- | :--- | :--- |
| `brand` | String | แบรนด์สินค้า (Required) |
| `modelName` | String | ชื่อรุ่นสินค้า (Required) |
| `description` | String | รายละเอียดสินค้าแบบยาว (Required) |
| `price` | Number | ราคาสินค้า (ต้อง >= 0) |
| `image` | Object | `{ url: String, publicId: String }` |
| `sku` | String | รหัสสินค้า (Unique) |
| `category` | String | Notebook, Keyboard, Computer, Monitor, Gaming Mouse, Graphics Card, RAM, CPU, Mainboard |
| `tags` | Array[String] | แท็กสำหรับช่วยค้นหา (เช่น "Gaming", "Wireless") |
| `stock` | Number | จำนวนสินค้าในคลัง (ต้อง >= 0) |

> **💡 Tag Processing Note:** ในการส่งข้อมูลผ่าน `FormData` ฟิลด์ `tags` จะถูกส่งเป็น **JSON-stringified Array** (เช่น `["Tag1", "Tag2"]`) Backend จะต้องใช้ `JSON.parse()` ก่อนบันทึกลงฐานข้อมูลเพื่อให้ข้อมูลถูกจัดเก็บเป็น Array ที่ถูกต้องตาม Schema
| `status` | String | 🆕 สถานะสินค้า: `active`, `inactive`, `draft` (Default: `active`) |
| `isFeatured` | Boolean | 🆕 สินค้าแนะนำ (สำหรับโชว์หน้าแรก) (Default: `false`) |
| `specifications` | Map (of String) | สเปกย่อยแบบ Dynamic (เช่น `{"RAM": "16GB", "CPU": "i7"}`) |

---

## 🛡️ 2. Validation Rules (Middleware)
ระบบมีการคั่น `productValidationMiddleware` และ `queryValidationMiddleware` เพื่อตรวจสอบข้อมูล:

### **ฝั่งการส่งข้อมูล (POST / PATCH)**
*   **Create:** บังคับส่งทุกฟิลด์ ยกเว้น `tags`, `specifications`, และ `isFeatured`
*   **Image:** ต้องมีการอัปโหลดไฟล์ภาพเสมอ (ยกเว้น PATCH ที่ไม่ต้องการเปลี่ยนภาพ)
*   **Numbers:** `price` และ `stock` ห้ามติดลบ
*   **Specifications:** ต้องเป็น JSON Object ชั้นเดียวเท่านั้น (ห้ามส่ง Object ซ้อน Object)

### **ฝั่งการดึงข้อมูล (GET)**
*   `minPrice` / `maxPrice`: ต้องเป็นตัวเลขและห้ามติดลบ
*   `page` / `limit`: ต้องเป็นตัวเลขตั้งแต่ 1 ขึ้นไป
*   `sort`: รองรับ `price_asc`, `price_desc`, `newest`, `oldest` (Default: `newest`)

---

## 📡 3. API Routes (Endpoint Map)

### **🛒 สำหรับ User (Public)**
| Method | Endpoint | Description | Query Params |
| :--- | :--- | :--- | :--- |
| **GET** | `/api/v1/products` | ดึงรายการสินค้าทั้งหมด | `keyword`, `category`, `brand`, `minPrice`, `maxPrice`, `page`, `limit`, `sort`, `spec_...`, `isFeatured` |
| **GET** | `/api/v1/products/:id` | ดึงรายละเอียดสินค้า 1 ชิ้น | - |
| **GET** | `/api/v1/categories` | 🆕 ดึงรายการหมวดหมู่ที่มีทั้งหมด | - |
| **GET** | `/api/v1/brands` | 🆕 ดึงรายการแบรนด์ที่มีทั้งหมด | `category` (Optional) |

### **🔒 สำหรับ Admin (Protected - Required Admin Token)**
| Method | Endpoint | Description | Body Content (FormData / JSON) |
| :--- | :--- | :--- | :--- |
| **GET** | `/api/v1/admin/products/stats` | 🆕 สรุปข้อมูลสต็อก (Total, Low Stock, out of stock) | - |
| **POST** | `/api/v1/admin/products` | สร้างสินค้าใหม่ | `FormData` (รวมไฟล์ภาพ) |
| **PATCH** | `/api/v1/admin/products/:id` | อัปเดตสินค้าแบบบางส่วน | `FormData` หรือ `JSON` |
| **PUT** | `/api/v1/admin/products/:id` | อัปเดตสินค้าทั้งหมด | `FormData` หรือ `JSON` |
| **DELETE** | `/api/v1/admin/products/:id` | ลบสินค้าและรูปภาพ | - |

---

## 💡 4. Advanced Features for Frontend

### **🔍 Dynamic Spec Filtering (GET)**
Frontend สามารถกรองสเปกย่อยได้โดยการส่ง Query ที่ขึ้นต้นด้วย `spec_`
*   ตัวอย่าง: `?spec_RAM=16GB&spec_Color=Red`
*   ระบบรองรับการส่งเป็น Array (Multiple Select): `?spec_RAM=8GB&spec_RAM=16GB`

### **🛠️ Smart Partial Update (PATCH)**
การอัปเดตสเปกย่อยทำได้โดยส่งเฉพาะ Key ที่ต้องการเปลี่ยน:
*   **เพิ่ม/แก้ไข:** ส่ง `{"specifications": {"RAM": "32GB"}}` -> ระบบจะแก้เฉพาะ RAM ตัวอื่นยังอยู่
*   **ลบออก:** ส่ง `{"specifications": {"Storage": ""}}` -> ระบบจะ **ลบ** Key "Storage" ออกจาก Database ทันที

### **📦 Pagination Response**
Frontend จะได้รับข้อมูลการแบ่งหน้าดังนี้ เพื่อนำไปวาดปุ่ม Pagination:
```json
{
  "success": true,
  "total": 50,       // จำนวนสินค้าทั้งหมดที่ค้นเจอ
  "page": 1,        // หน้าปัจจุบัน
  "totalPages": 5,  // จำนวนหน้าทั้งหมด (เอาไปวาดปุ่ม 1,2,3,4,5)
  "data": [...]     // รายการสินค้า 12 ชิ้น
}
```

---
*Generated by Gemini CLI Agent - Project Jamine*
