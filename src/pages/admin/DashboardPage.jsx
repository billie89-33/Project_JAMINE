import DashboardModule from '@/modules/admin/dashboard';

/**
 * 🚀 DashboardPage (Admin)
 * หน้าสรุปสถิติหลักแบบ Full-width
 * (Layout ส่วนกลางถูกจัดการโดย AdminLayout แล้ว)
 */
const DashboardPage = () => {
    return (
        <DashboardModule />
    );
};

export default DashboardPage;
